# Get repositories' links from all projects in bitbucket

This script will generate a list of links to repositories that belong to each project that exists in bitbucket

* Install the requirements
```
pip3 requests
```

* Run the script